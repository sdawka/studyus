# Remediation implementation plan — 2026-09-07

Approved in conversation; source specifications are audit-2026-09-07.md and security-audit-2026-09-07.md. Follow the explicit decisions in decision_log.md. No production deployment or production security probes; push reviewable PRs, no merge without green CI and authority.

## Delivery tasks

1. **Atomicity (Sol):** tasks/assessment ownership preflight then atomic parent/link/event/mastery/notification/task transition; convert four expected failures and exercise denied side effects.
2. **Recovery basics (Terra):** note revision/serialized coalesced saves; agenda only obligations overdue; preserve failure drafts. Convert two expected failures, test new edit/title/link and out-of-order/retry paths.
3. **Safe downloads and URLs (primary):** binary download, RFC5987 filename, nosniff/private cache, owner denial; HTTP(S)-only central validator and disabled invalid historical navigation. Convert two expected failures.
4. **Seed/CI foundations (Luna):** clean migrate/reseed, learner-namespaced synthetic IDs, two-user fixture support; separate auth-independent and Clerk browser jobs; primary owns workflow/config integration.
5. **Account lifecycle foundation (Sol):** additive D1 account state, deletion-event/job and runtime registries, active-email uniqueness; DO terminal fence/retention, revoke feeds/background/member access, operator-only tooling, no auto relink. Signed replay-safe Clerk webhook and late-operation tests.
6. **Upload lifecycle/quotas (Terra):** bounded multipart parser, 10MiB/file +250MiB/100 per account, atomic pending reservation, ready/deleting rows, reconciliation and compensation; group storage later uses same lifecycle separate ownership.
7. **Telemetry/AI budgets (Sol):** atomic100/session cap, trusted conversions backend only, DNT early exit, edge10batches/min,30day retention; AI disabled,DO30calls/UTCday,1concurrent,1000tokens,30sdeadline, attempted-call accounting and leased reservations allproviderentrypoints.
8. **Durable timing (Terra):** separate acknowledged active duration,15sheartbeat with sequence/lease, stale gaps excluded, one active device explicit takeover, old open recovery list, exactlyonce complete/discard preserved.
9. **UI polish (Luna):** canonical sign-in alias and safe return URLs, shared auth loading/error wrapper, account authenticated shell; persistent trial labels/selected/no-results/back/mobile reset; grouped destinations/local icons; current-course grades focused edit; desktop nonmodal popover/mobile sheet keyboard contract; verify tabs/tables before altering intentional scroll.
10. **Calendar reliability (Terra):**5/user/tick global100,7day done retention,10retry limit/30day failed retention +explicit retry; provisioning ledger/provider marker/compensation, deleted-account fence.
11. **Planning foundation (Sol):** availability/capacity/estimates/locks/revisions/changeledger; deterministic14day scheduler,explicitpriority/deadline/learningevidence,25min editable default estimate, preserve durations/locks/active/history/external, timezoneDST, weeklycapacity, no silent drops. Preview/apply/undo source-revision guarded, coalesced missed/change jobs.
12. **Planning UI and preview (Terra/Luna):** optin setup, moved/unplaced reasons, conflict-safe undo; shared pure grounded recommendations for suppliedtopics/catalog/deadlines/capacity, no invented mastery/prerequisites, local simulations, working truthful marketingdemo.
13. **Groups foundation (Sol):** distinct groups/members/invites/resources/files/events/RSVP; owner/member perms,7day emailbound singleuse tokenhash+keyedemail,25members/5ownedgroups,250MiB/100files group; retained group-owned snapshots, personal keys never exposed; deleted attribution/member detach, ownerlessread-only, operator reassignment.
14. **Groups UX/integration (Terra/Luna):** Personal/Groups nav, invite/member/resources and real organizer/RSVP, membereditown/ownermoderate; accepted sessions busy in personalplanner, no personalcalendar disclosure or automatic learningevidence; groupfilecopy pending/ready/delete.
15. **Platform hardening (primary/Sol):** compatible dependency patches/current consumer review; CSP/framing/nosniff/privatecache; redacted errors/feedtraces; prepare TLS1.2/HTTPS/stagedHSTS (production approval separately); pinnedCIactions/leastprivilege/no untrustedPRsecrets.
16. **Verification (primary + independent Sol + Luna browser):** all9expectedfailures promoted when fixed; realD1/R2/DO tests races/auth/deniedsideeffects/deletioninflight; browser onboarding/import, tasks/grades/note/study/quiz/planner/groups/account failure paths;3themesxlightdark,320/390/430/820/desktop,200%zoom/reducedmotion/keyboard/screenreader/contrast; isolated Clerk fallback ifAgentTasks unavailable; sandboxprovider separate. Types/build/annotations/cleanseed/CI allgreen. Update existing audits with evidence and honest gaps.

## Ownership and sequencing

Primary owns schema.ts, all numbered D1 migrations, shared API contracts, dependency manifests, Wrangler and CI configuration. Agents request contract edits through primary and write only assigned paths. Three concurrent writers maximum, one review seat. No commits/pushes/deploys/children from agents. Primary commits exact accepted paths, pushes PRs; never sweep untracked user .agents. Shared dev slot is primary-owned; never build/types while browser dev server lives. Use rtk for shell, WRANGLER_LOG_PATH in /tmp; Node checked24.20.0.

## Integration defaults

New features disabled until their acceptance gates pass; AI remains disabled. Additive migrations retain rows, registry names are internal deterministic learner:<id>. Group sharing copies files into group scope so retained contributions do not bypass private ownership. Account deletion is monotonic; provider operations and late writes recheck active state. Active-email uniqueness allows new accounts without restoring retained data. Existing completed study history unchanged; unknown historical active duration remains unknown rather than fabricated. Old unfinished sessions remain recoverable but only one active lease exists. No restoration/public operator endpoint.
