# studyus Screen Inventory (v1.4)

> **Docs overlay sync**: this file's route table and "Shell components" list are mirrored into
> `src/lib/docs-overlay/annotations.ts` (the temporary in-app docs overlay — see
> `docs/product/annotations.md`). Changing a route's purpose or a component here updates that
> registry in the same commit; `npm run check:annotations` catches drift between the two.

Every page renders inside `AppShell.astro` — on desktop (>767px), a two-column grid: a collapsible **Sidebar** (Home, current-term courses with hue tints, "+ Add course", past-terms `<details>`, footer Feed/Settings/collapse toggle) and a sticky **Header** (56px; a center slot for course tabs/breadcrumbs, and a right-hand cluster: Record Event pill, scratchpad popup, todo dropdown, notifications bell, avatar menu). Below 767px the shell swaps to a different paradigm entirely — see "Mobile (≤767px)" below and `docs/design/mobile-shell.md` for the full contract. `ThemeScript.astro` stamps `data-theme`/`data-scheme`/`data-sidebar` on `<html>` pre-paint from `localStorage`, mirrored server-side from `users.settings`. `*` marks Svelte islands.

## Mobile (≤767px)

Below the mobile-shell breakpoint (`@media (max-width: 767px)`), every page keeps its content but the shell around it changes: the Sidebar is gone, replaced by a fixed **bottom tab bar** (Home · Tasks · [Record FAB] · Planner · Courses — `BottomNav.astro`); the header slims to a "su" mark + course tab strip + bell/avatar (Record-event pill, todo, and scratchpad have no tab of their own — they move into the avatar's bottom sheet, which also gains Feed). Per-page notes:

- **`/dashboard`**: mobile reorder leads with **TodayTasks** (the hallway job — check things off), then the week strip, deadlines, course cards, record-event form last (desktop keeps its two-column grid).
- **`/planner`**: stops being a centered modal and becomes a full page between the header and tab bar (no scrim, no close button — dismissed by tapping another tab). Defaults to **Agenda** view instead of the week grid; when a day/week view is shown, `WeekGrid` collapses to a container-measured 1-day (<480px) or 3-day (<760px) rolling window anchored on today. `EventPopover`/`CreateSessionPopover` render inside a bottom sheet instead of an anchored popover.
- **`/tasks`**: same full-page treatment as `/planner` (clone of its modal-to-page pattern). Filter chips and inline-add wrap for touch; checkboxes and delete buttons grow to ≥44px hit targets.
- **`/courses/[slug]*`**: course tab strip becomes a horizontally-snapping, edge-faded row in the mobile header; the Overview's two desktop stacks interleave into one column (≤836px container, via `display: contents` + `order`): To do → Attendance → Understand next → Coming up → Standing → Assessments → Mastery → Practice → RecentActivity → About (hallway jobs first). `AssessmentsCard`'s table remaps to a card grid at the narrowest widths (thead hidden, CSS grid `<tr>` areas).
- **Everywhere else** (feed, notes, profile, settings, onboarding, etc.): grid/table guards so nothing overflows; no bespoke reorder beyond that. See the plan's Part 2 for full per-page detail.
- **Header popovers → sheets**: the bell and avatar open as bottom sheets (`Sheet.svelte`, portaled, scrim + bottom-anchored panel) instead of anchored popovers; todo/scratchpad triggers are hidden (no tab or sheet of their own — Tasks tab and the avatar sheet cover them).

| Route | Purpose | Key Components |
|---|---|---|
| `/login` | Session login | LoginForm* |
| `/` | Redirect → onboarding or dashboard | (redirect logic in middleware) |
| `/onboarding` | 4-step stepper (skippable): what studyus is → how mastery is modeled → confirm imported courses → set display name + current term (`PATCH /user`, stamps `onboarded_at`) | OnboardingFlow* |
| `/dashboard` | Task-centric (v1.4) — page head (kicker date·term·week, headline that re-keys tasks-first: overdue non-`attend_class` task → today's task → nearest deadline) → main column (WeekView island — collapsed 7-day strip by default, expands to a full week grid, state persisted via `sb:weekview`; **TodayTasks** — checkable hero: Overdue/Today/Next-7d buckets plus a collapsed "Catch up" disclosure for past `attend_class` rows, and a bottom row of one-tap wellness chips that mint a plain today-todo; CourseCards, now slimmed to hue/code/title + a grade pill + one muted meta line ("N tasks open · next due Fri") — mastery/assessment-progress moved off the card face) + rail (RecordEventCard, **DeadlinesList** — assessment-due items only; overdue-task nagging now lives in `TodayTasks`/`grade_entry` tasks instead). `DeadlinesList.astro` is the v1.4 rename of the old `DueList.astro`. | WeekView*, TodayTasks*, CourseCards, RecordEventCard, DeadlinesList |
| `/planner` | Full-viewport modal layer over AppShell (scrim + near-fullscreen panel; Escape/close → back or `/dashboard`). Defaults to a week time-grid view (`WeekGrid`, per the frozen spec in `docs/design/planner-ux.md`); toggles to month (`CalendarGrid`) or agenda (`AgendaList`) via the same view switcher, with a current-term/all-courses/single-course filter and course-hue chips throughout. Clicking any item opens `EventPopover`; clicking empty grid space opens `CreateSessionPopover` for an inline scheduled-session create. `PlannerRail` lists unscheduled tasks/assessments due soon alongside the grid — click jumps the grid to that item's week, but the rail itself doesn't support click-to-schedule or drag-to-schedule yet (deferred, see `docs/todo.md`). Supports deep links via `?event=<id>&date=<iso-date>` query params, and month/agenda cells click through into the week view on the target date. No delete affordance for scheduled study sessions yet (the sessions API only has create + `PATCH .../complete`, no `DELETE`). | PlannerView*, WeekGrid*, CalendarGrid*, AgendaList*, EventPopover*, CreateSessionPopover*, PlannerRail* |
| `/calendar` | 302 redirect → `/planner` (kept alive for old links/bookmarks) | — |
| `/courses` | Course cards grouped by term (current term first), grouped by mastery/status; archived courses collapsed in an "Archived" `<details>` section below | MasteryBar |
| `/courses/[slug]` | `CourseLayout.astro` renders the course head + 6-tab bar (underline style, `aria-current`) | CourseLayout |
| `/courses/[slug]` (Overview) | **v1.8 task-oriented rebuild** — answers "what do I do for this course now?" before "how am I doing?". Main column leads with **To do** (this course's open tasks in the same Overdue/Today/Next-7-days idiom as the dashboard's Today card, quick-add pre-scoped to the course), then **Understand next** (up to 4 KCs worth an absorb session: weakest started KCs, stalest breaking ties with an "idle Nd" pill, plus a guaranteed slot for the next untouched KC in curriculum order — v1.9: the "new" slot and weak-pool ordering now prefer KCs with all prerequisites ready over ones still blocked, and a blocked pick renders "unlocks after ⟨prereq name⟩" instead of a bare gray-out; each row links KC detail + `/learn/[kcId]`). Rail: attendance record, **Coming up** (nearest dated official assessments w/ weight pills + urgency), **Standing** (the old weighted-grade hero demoted to a compact stat card, anchor-links to the ledger), practice-progress readout, recent events. Reference material sinks below the fold in main: full assessments ledger w/ inline grade entry + "Concepts covered" KC multi-select (v1.4, links `assessment_kcs`), mastery per branch, and the course overview text as a muted "About this course" block. Retired: `StandingTab`, `TasksCard`, `DeadlinesCard`. | CourseHome*, CourseTasks*, UnderstandNext*, UpNextCard*, GradeStatCard*, AssessmentsCard*, MasteryCard, AttendanceCard*, PracticeCard*, RecentActivityCard* |
| `/courses/[slug]/concepts` | Branch/KC tree with mastery bars, KC detail link. v1.7: each KC row also gets an "Understand" link into `/learn/[kcId]`. | — |
| `/courses/[slug]/notes` | Linked notes + AttachmentsPanel; notes saved from the header scratchpad to this course also land here | NotesList*, AttachmentsPanel* |
| `/courses/[slug]/resources` | Canonical/feed/user-shared resource groups + this course's feed section | ResourceCard |
| `/courses/[slug]/practice` | Drills island: StudyFlow (preselected to this course, skips the course-pick step) + QuickQuiz + links to recall/worked-example tutor for fact/rule KCs. **v1.9**: StudyFlow gains an optional ritual picker at session start (active `session_shape`/`both` rituals only) — picking one renders its `steps` as a guidance rail (not enforced gates: 'retrieval' links QuickQuiz, 'new_material' links `/learn/[kcId]`, 'game' links the course's `/play` tab, 'reflect' reuses the existing reflection field) and stamps `study_sessions.ritual_id` on completion. | StudyFlow*, QuickQuiz* |
| `/courses/[slug]/play` | Exploratory island: lists the course's principle/concept KCs → interactive-model/self-explain tutor conversations, with history via `GET /tutor/conversations?course=`. v1.7: each KC row also gets an "Understand" link into `/learn/[kcId]`. | ScaffoldChat*, InteractiveModel* |
| `/courses/[slug]/kc/[kcId]` | Mastery history, event timeline, linked notes/resources, "Tutor me" (v1.7: alongside a new "Understand" link into `/learn/[kcId]`) | MasteryChart*, EventTimeline*, TutorButton |
| `/study`, `/study/quiz` | Kept alive, unlinked — functionality absorbed into course Practice tabs | StudyFlow*, QuickQuiz* |
| `/notes`, `/notes/[id]` | Kept alive, unlinked from the sidebar (replaced by the header scratchpad popup for quick capture); full editor still reachable via a note's "open full editor" link | NoteList*, NoteEditor* |
| `/tasks` | v1.4 rebuild: full-page route-modal over `AppShell` (clone of the `/planner` modal pattern — scrim + panel, Escape/close → back or `/dashboard`). Filter bar (All / per-course chip w/ hue dot / Other) syncs `?course=` via `history.replaceState`. Card grid, one per course (course-hued top border, "N open" head) + a neutral "Other" card, always last; each has inline add (ghost "+ Add task" → title/date/Add) and one level of subtasks (chevron + "N/M" progress pill, "+ Add subtask" row; checking a parent completes its remaining children). Past `attend_class` rows sink into an idle "Catch up" cluster; completed tasks collapse into a "Done (N)" disclosure. Also still surfaced as the header's `TodoDropdown` (top ~7, add-task inline) for quick capture without leaving the current page. **v1.8**: rows are content-only at rest — the urgency pill, course CODE chips and `auto` chip sit on a meta row below the title, and snooze/delete/log-practice move into a floating icon cluster revealed on row hover (fine pointers), on `:focus-within` (keyboard), or via a persistent `⋯` disclosure on touch. Completing a task holds the row in place ~1.6s (check, strikethrough, confetti) before it glides out. | TasksView*, TaskItem*, TaskQuickActions*, TaskTypeIcon* |
| `/learn/[kcId]` | v1.7: guided "absorb" experience for one KC, a 4-stage flow. **Stage 1** — layered prereq map (`PrereqGraph`): the KC's prerequisites grouped by BFS depth (1..N hops out), each row showing ready/not-ready per the mastery-threshold rule, with "Verify N weak prerequisites" and "Continue anyway" actions (a leaf KC with no prerequisites skips straight to Stage 4). **Stage 2** — verify (`VerifyQuiz`): a `quick_quiz` scoped to exactly the weak prerequisites via explicit `kc_ids`. **Stage 3** — tap-to-rank interest ordering (`InterestRanker`): student orders the KCs they're about to cover by interest, producing `focus_order` (target KC always appended last). **Stage 4** — absorb chat (`AbsorbFlow` wrapping `ScaffoldChat`, `mode: 'absorb'`): correction proposals render as inline accept/dismiss cards; accepting posts to the corrections ledger. | PrereqGraph*, VerifyQuiz*, InterestRanker*, AbsorbFlow*, ScaffoldChat* |
| `/corrections` | v1.7: dedicated corrections-ledger page (`CorrectionsLedger`) — Active / Internalized / All filter tabs; each row shows the linked KC, the prior belief (struck through), the correction text, and the accepted date; "Mark internalized" has an inline two-step confirm. Reachable from the desktop sidebar and, on mobile, the avatar-menu sheet (next to Feed — no bottom-nav tab of its own, per the mobile-shell convention). | CorrectionsLedger* |
| `/feed` | Curated + user-shared resources in a Pinterest-style masonry grid (5 columns at 1440px down to a single column under 600px); each card shows a favicon tile fetched from the link's hostname; course chip-filter bar (All + one chip per current-term course) above the grid; add-resource form | ResourceCard*, ShareResourceForm* |
| `/grades` | Kept alive, unlinked — content absorbed into dashboard + course Overview | GradeTable* |
| `/profile` | **v1.9 — the learner identity page**, rebuilt from a flat mastery+streaks readout into a section-ordered story: **Overall standing** (mastery %, current/longest streak) → **Capabilities** (competency list with derived mastery/coverage bars, plus a meta-skills strip — retrieval practice / self-explanation / error analysis, each a frequency+trend readout, deliberately never a 0–100 score) → **Learning frontier** (replaces the old "Global knowledge map — coming later" stub: unmastered KCs across all courses whose every prerequisite is already ready, grouped by course, each linking `/learn/[kcId]`; blocked/mastered counts shown as one summary line, not a per-KC breakdown) → **Rituals** (CRUD + adherence — done/skipped/upcoming dot row for recurring rituals, a plain usage count for session-shape ones; no streaks or badges) → the pre-existing **Mastery by course** / **Concepts by status** / **Recent activity**. | StandingCard, CapabilitiesPanel*, FrontierPanel*, RitualsPanel*, MasteryByCourse, ConceptsByStatus, RecentActivitySection |
| `/settings` | Theme (compass/focus/campus) + scheme (light/dark/system) pickers, "Automatic tasks" (v1.4 — one row per sweep-generator family with a checkbox, PATCHes `settings.task_generators`), display name/current term, logout | AppearanceSettings*, TaskGeneratorSettings* |

## Shell components

- **Sidebar.astro** — brand, Home, **Tasks** (v1.4, between Home and Courses), current-term courses (two-line code+title, hue-tinted left rail when active), "+ Add course" (always visible), past-terms `<details>`, footer (Feed, **Corrections** (v1.7), Settings, collapse toggle). Collapses to a 60px rail of monogram chips, persisted via `localStorage`/`settings.sidebar_collapsed`.
- **HeaderActions.svelte** — island composing the Record Event pill (opens `LogEventModal`), `ScratchpadPopup` (saves a note to general or a specific course), `TodoDropdown`, `NotificationsBell` (60s visibility-aware poll), `AvatarMenu` (Profile/Settings/Logout; mobile also gains Feed and **Corrections** (v1.7) here — neither has its own bottom-nav tab, per the mobile-shell convention).
- **CourseLayout.astro** — wraps `AppShell`, renders the course head + 6-tab bar for all `/courses/[slug]/*` subroutes.
- **AddCourseModal.svelte** — triggered from the sidebar's "+ Add course" button (and the dashboard empty-state) via a `window` CustomEvent; posts to `POST /courses`.
- **SessionMiddleware** — `src/middleware.ts` gates pages and `/api/v1` routes; redirects unauthenticated users to `/login`.

## TODO

- Detailed component APIs (props, events, internal state management).
- Design system / component library documentation beyond `docs/architecture/overview.md`'s tokens section.
- Wireframes or mockups for key screens (planner, Play tab interactive models).
