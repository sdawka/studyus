node_modules/
dist/
.astro/
.wrangler/
.dev.vars
.DS_Store

# Generated by `wrangler types` (npm run check:types regenerates it) — regen
# rather than commit so it can't go stale against wrangler.jsonc.
worker-configuration.d.ts
